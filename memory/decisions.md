# Decision log

> Append-only. Newest entry at top. Each entry: date, decision, reasoning, reversibility.
> Never rewrite or reorder an existing entry; a changed mind gets a new entry that supersedes the old one.
> This file ships with one real worked entry so you can see the shape. Keep it or delete it.

## 2026-07-20 · Added Claude Code SessionStart/SessionEnd lifecycle hooks, the one Cognee trait chosen to adopt
**Decision:** Added two new hook scripts, `.claude/hooks/molt-session-start.sh` and `.claude/hooks/molt-session-end.sh`, that run `molt-verify.py` automatically at the start and end of every Claude Code session, plus a new `ensure_claude_hooks_settings` function in `molt-init.py` that registers them in `.claude/settings.json` without disturbing any hooks an adopter already has configured there. `molt-init.py` now embeds these two scripts (base64, same pattern as the other five embedded files), and `check_init_embed_consistency` / `scripts/build-molt-init.py` were extended to cover them, so this system's own drift-detection now watches these files too.
**Reasoning:** Comparing Molt against Cognee (topoteretes/cognee, a knowledge-graph/vector-search memory platform) surfaced one trait worth adopting: automatic, enforced memory checks via Claude Code's own lifecycle hooks, instead of relying on the agent to remember to run `molt-verify.py`. Everything else about Cognee (vector search, LLM-based auto-distillation, cloud hosting) would have meant giving up Molt's zero-dependency, no-network-call identity, so only this one trait was taken, and only after reading Claude Code's actual current hooks documentation (`SessionStart` matcher values, plain-stdout-as-context behavior, `SessionEnd`'s no-blocking/stderr-only behavior) rather than assuming the schema. The settings.json-merge logic was tested against three real scenarios: a clean directory (fresh file created), a directory with a pre-existing `settings.json` containing an unrelated `PreToolUse` hook and someone else's own `SessionStart` script (both preserved, Molt's two hooks appended alongside), and a second run against the same merged file (no duplicate entries, reported "already registered"). This is scoped explicitly, in the README and in the scripts' own comments, to Claude Code only; it does nothing for Cursor, Copilot, or any other tool, unlike the git pre-commit hook and CI workflow which work everywhere.
**Reversible:** Yes. Delete `.claude/hooks/` and the `hooks` block in `.claude/settings.json`, or simply don't run this step of `molt-init.py`; the core memory files and `molt-verify.py` itself don't depend on this at all.
**Review:** Revisit if Claude Code's hooks JSON schema changes in a breaking way, or if an equivalent lifecycle-hook mechanism appears in another tool and this should be extended to cover it too.
**Hash:** 68385f9ce658c4a6f94d3a9807880e083051e386db758e6dd774672730cba4d6

## 2026-07-20 · Reframed the one honest weakness row in README's comparison table, instead of hiding it
**Decision:** The comparison table's last row ("Built for thousands of autonomous agents": no / yes / no) was flagged as a real weakness worth addressing before wider posting. Rather than removing it or softening the honesty, changed Molt's cell from a flat "no" to "not the goal," extended the explanatory line underneath to state directly that adding fleet-scale support would require giving up the zero-dependency, plain-markdown properties that make every other row true (you can't win that row without losing the ones that matter to Molt's actual audience), and linked to `SCALE-SPEC.md`, which already contains real reasoning (a comparison against agentmemory, Headroom, and claude-mem specifically on this scale question) for anyone who wants the deeper argument instead of just the README's word for it. No claim was softened or removed; the "no" is still there, just no longer presented as an unexplained loss.
**Reasoning:** Direct instruction: figure out how to handle the one row where Molt honestly loses. The right move for a project whose entire premise is "prove it, don't just claim it" is to explain a real limitation clearly, not to bury or spin it; a table that claims to win everything would undercut the project's own credibility argument. `SCALE-SPEC.md` already had the deeper reasoning written; this just surfaces it at the exact point a skeptical reader would want it.
**Reversible:** Yes. Wording and a link only; the underlying comparison and the decision not to chase fleet-scale support are unchanged.
**Review:** None.
**Hash:** 8f9c940c911dd9ecaa2088031e120d2c04774fc11e67d0a5c02f7eecc9d7e6d7

## 2026-07-20 · Made README.md explicit that Molt is for existing setups too, not only fresh ones
**Decision:** The "Who this is for" section previously led with the fresh-start case ("if you just picked up Claude...") and only implicitly covered existing users through the scenario bullets underneath. Added an explicit line up front ("this isn't only a fresh-start tool, it works the same way whether you installed five minutes ago or you've had Claude Code running for months") and a new paragraph for people already mid-use: dropping Molt in doesn't erase anything, it reads an existing `CLAUDE.md` and never overwrites it, so months of accumulated rules stay intact and just get memory and a checker added on top retroactively. Reworded the transition into the scenario bullets to "Either way, here's what it actually fixes, day one or day two hundred" so the list reads as shared ground rather than beginner-only. This is a real, already-true behavior of `molt-init.py` (the write_if_absent/actual_claude_md logic fixed earlier this session), just not previously stated as a selling point for this specific audience.
**Reasoning:** Direct instruction: make sure the README doesn't read as only for people brand new to Claude/AI tools, since it's equally valid, and arguably more immediately valuable, for someone who's already deep into a setup and has already felt the exact pain points described.
**Reversible:** Yes. Documentation-only; the underlying non-destructive install behavior was already true and tested, this only makes the claim explicit in the README.
**Review:** None.
**Hash:** 3f11b288d7621779daf86e307916705eff2e51253166ba56973e40af37159f7f

## 2026-07-20 · Called out minimal token use as a default in README.md, not an opt-in feature
**Decision:** Strengthened the existing "you never read the whole history" bullet in "Why it works the way it does" into "Low token use isn't a setting, it's the default," and explicitly named that this is mechanically enforced, `molt-verify.py`'s token-efficiency check, not just a design intention that could quietly stop being true. Added one matching badge ("token use: minimal, by default") pointing at the same section. No new section added, kept the trim from the previous pass intact.
**Reasoning:** Direct instruction: make sure the README states that minimal token consumption comes as the default, not something a user has to configure. This was previously implied (progressive disclosure, the INDEX.md-first read order) but never stated as plainly as the direct claim it actually is, and it's backed by a real mechanical check, so it's a fact worth stating outright rather than leaving a reader to infer it.
**Reversible:** Yes. One bullet strengthened, one badge added; nothing else changed.
**Review:** None.
**Hash:** 1db2684d77b975bc7e5bc9731dbce275fd994e118758b3433a7968f9292f00f4

## 2026-07-20 · Trimmed README.md before the first real public post, cutting redundancy without losing content
**Decision:** After several additive passes (beginner section, real-situations section, game-changer section, wit, demo output), README.md had grown to 222 lines across 17 sections with real overlap: three separate sections were all making variations of the same "here's why you should trust this" pitch back to back. Merged "If you just picked up Claude, start here," "Real situations this actually fixes," and "Why this can be a genuine game changer" into one section, "Who this is for, and what it actually fixes," trimmed from 6 scenario bullets to 5, and cut the standalone "Zero to hero, in one command" section entirely since it repeated the exact one-liner already shown at the top with no new information. Also moved the real demo output earlier (right after the one-sentence problem statement, instead of after a "what Molt actually does" section that just restated the same idea in different words, cut that section too) so proof appears sooner. Shortened the comparison table from 7 rows to 5, dropping the two least decision-relevant rows ("extra software needed," "easy to read and fork") since they were restating points already made elsewhere. Trimmed one badge (Python-version badge) since two dependency-related badges said close to the same thing. Result: 165 lines, 13 sections, same fully-preserved facts (43 cases, 13 checks, zero dependencies, day-one framing, the first-project disclosure, all FAQ content), no anchor links broken. Full audit and both benchmark scales pass.
**Reasoning:** Direct prompt: about to actually post this on Hacker News, Reddit, Twitter, etc., and asked for an honest opinion first. A skimming reader on those platforms decides whether to keep reading in the first several seconds; three sections making a similar case back to back, plus a repeated one-liner, costs attention before the strongest material (the real demo, the 43-case benchmark) gets seen. Trimming before the first real public exposure, rather than after, is the version of "measure twice, cut once" that actually matters here.
**Reversible:** Yes. Documentation-only; every fact this file previously stated is still present somewhere in the trimmed version, just consolidated. The removed prose can be restored from git history if the trim ever feels like it went too far.
**Review:** Once this actually gets posted somewhere and gets read by strangers, revisit based on real reactions, not just my own judgment of what should land.
**Hash:** 37d02ac2be33f696179be54a426ac2e12c2a3231fc483d73dfd700c22cc991cd

## 2026-07-20 · Added a beginner-first section to README.md, positioning Molt as the right day-one setup
**Decision:** Added a new section right after the opening disclosure block, "If you just picked up Claude, Cursor, or any AI coding tool, start here," aimed specifically at people brand new to AI coding agents rather than people already frustrated with an existing setup. The pitch: most people configure an agent with nothing, hit the familiar pain points weeks later, then go looking for a fix; running the one-liner before any of that happens means day one is already the good setup, no bad habits to unlearn first. Added a matching badge ("good for: Day One setup") linking to the new section, and verified the anchor resolves correctly. No existing facts, sections, or other wording changed.
**Reasoning:** Direct instruction: make clear this is mainly for people fresh to a tool like Claude who want the best possible setup from the start, not only for people already dealing with the problems described elsewhere in the README. That's a distinct audience angle (prevention, not just cure) worth naming explicitly rather than leaving it implied.
**Reversible:** Yes. Documentation-only addition; nothing else in the project reads this section.
**Review:** None.
**Hash:** f58d9d4aeabe33391bb3159b33ac1e1de44fe5f1fada100aa50c45a1c3bcac1b

## 2026-07-20 · Added personality (wit) to README.md and disclosed it's a first GitHub project
**Decision:** Layered wit into README.md's existing prose in a handful of targeted spots rather than rewriting it wholesale: the opening disclosure block ("this is my first-ever public GitHub project... I skipped hello world and went straight to prove it"), a sharper line in the trust-problem framing, a couple of dry asides in the "game changer" section, one line in "the bug that made this real," a new FAQ entry addressing the first-project question directly and honestly, and a closing line in the footer signature. Every factual claim, section, badge, and anchor link is unchanged from the previous pass; this was tone only, added in small doses so the humor doesn't crowd out the actual information, which is still the point of the page.
**Reasoning:** Direct instruction: make it witty, matching the requester's own personality, and disclose openly that this is a first GitHub project rather than letting a reader assume otherwise. The disclosure is framed as a strength (a first project built with a built-in adversarial test suite) rather than an apology, since that's the honest and more interesting framing.
**Reversible:** Yes. Documentation-only, no facts changed, easy to dial back if the tone ever reads as too much once it's actually live and being read by strangers.
**Review:** Revisit once real reader feedback comes in (Hacker News comments, Reddit replies); humor that reads well solo can misfire in front of an unfamiliar audience, and the honest thing to do is watch for that instead of assuming it landed.
**Hash:** 324045e86cca12740e983fe9fe6ddda58e9711a5474095dfdfed6b547ef9af86

## 2026-07-20 · Rewrote README.md in plainer language with concrete, story-shaped use cases
**Decision:** Rewrote README.md's prose (not its facts) to use simpler words throughout and replaced abstract bullet points with concrete, first-person-sounding scenarios ("I switched from one AI model to another and lost everything," "my agent told me it finished something, and it hadn't") mapped directly to what Molt does about each one. Added a "Why this can be a genuine game changer" section that argues the actual case honestly: not that the engineering is clever, but that it removes the need to take an agent's word for it, at a one-command setup cost. Reworded section headers to be more direct ("How it stacks up" instead of "How it compares," "Questions people actually ask" instead of "FAQ") and updated badge anchor links to match. Kept every factual claim identical to the previous version (43 adversarial cases, 13 checks, zero dependencies); this was a tone and clarity pass, not a claims change. Verified all badge anchors still resolve to their renamed headers. Full audit and n=300 benchmark still pass, no code touched.
**Reasoning:** Direct instruction: make the README more engaging, in simple words, with real use cases, framed as a potential game changer for whoever sets it up. The previous version was accurate but written for someone already comfortable with terms like "progressive disclosure" and "append-only log"; this version is aimed at someone who just wants to know why they should care, in the first ten seconds of reading.
**Reversible:** Yes. Documentation-only; no code, no facts changed, only how they're explained.
**Review:** None.
**Hash:** dfb5a2b2ac4f04e5e6305741a2d6d74c62de55e237224196ddc7809603c84cdd

## 2026-07-20 · Added CONTRIBUTING.md and linked it from README
**Decision:** Added `CONTRIBUTING.md`: how to report a bug, how to submit a PR (including the exact molt-verify.py/benchmark/build-molt-init.py sequence to run before opening one, matching what CI actually checks), and an explicit "what won't be merged" section (no dependencies, no network calls, no softening a FAIL into a WARN without an argued reason in the PR). Framed the highest-value contribution as trying to fool `molt-verify.py` and submitting the successful attempt as a permanent adversarial case, since that's the exact mechanism every real gap in this project has ever been closed by. Linked it from README's Contributing section instead of duplicating the guidance there. Social-facing assets (a repo social-preview image and launch copy for Hacker News/Twitter/LinkedIn/Reddit) were also produced this session but live outside this repo, not checked in, since they're one-time marketing artifacts, not project source.
**Reasoning:** Direct instruction: make the project accessible and adoptable by outside contributors, not just installable. A repo with no CONTRIBUTING.md signals nobody's expecting outside involvement; one that explains exactly what CI checks removes the most common first-PR friction (a contributor guessing what will make the build fail).
**Reversible:** Yes. Documentation-only addition; nothing else in the project reads or depends on this file.
**Review:** None.
**Hash:** 7d661bfd4a5aa6099673d4695b8b0d624e1100f20a5978765763cf47a8f5c3d8

## 2026-07-20 · Rewrote README.md for GitHub discoverability and beginner clarity, and fixed a real leak in install.sh
**Decision:** Rewrote README.md front to back: badges (license, zero-dependency, stdlib-only, adversarial-test-count, tool compatibility), the real GitHub one-liner hardcoded to the actual repo (no more `<you>` placeholder), a plain-English "problem this solves" section aimed at someone brand new to Claude Code/Cursor/Copilot with no jargon, a real captured terminal transcript (not a mockup) from an actual fresh install, a "who this is for" section mapping plain complaints ("my agent keeps re-asking things," "I don't trust what it says it did") to what Molt does about them, an updated file tree reflecting everything actually built since the original README (molt-init.py, molt-chain-append.py, molt-redact.py, the pre-commit hook, the CI workflow), an explicit call-out of the 43-case adversarial benchmark run at two scales (this was previously undocumented in README entirely despite being one of the strongest trust signals this project has), and a suggested repo description + expanded topics list tuned for GitHub/AI-agent search terms. While rewriting the install instructions, found a real bug in `install.sh`: it copied `memory/decisions.md` and `memory/INDEX.md` straight from this project's own working copy, meaning anyone running it from a real checkout would get THIS project's actual internal decision history dropped into their project instead of a fresh starter template. `molt-init.py` never had this bug since it always generates fresh templates. Could not delete `install.sh` (a sandbox file-permission restriction blocked `rm`), so rewrote it as a thin shim that forwards to `python3 molt-init.py "$@"`, verified the fix directly: running the shim now produces the correct fresh YYYY-MM-DD placeholder entry, not this project's 21 real ones. Full audit and both benchmark scales (n=300, n=3000) still pass with no regressions; no code paths in molt-verify.py or molt-init.py themselves changed.
**Reasoning:** Direct instruction: make the repo something people will actually understand, adopt with minimal effort, and want to share, without losing the honesty the whole project is built on (no invented demo output, no unsubstantiated claims). The install.sh bug was found by actually reading the file while updating what it should say, not by a planned audit; worth fixing immediately since a stale, buggy secondary install path undermines the exact trust story the README is now making louder.
**Reversible:** Yes. README.md and install.sh are documentation/convenience surfaces; nothing else depends on their exact wording. The GitHub description/topics suggestion is a comment block, not applied automatically, Anuvind still pastes it into repo settings by hand.
**Review:** Once the repo is actually pushed and public, revisit whether the badges (especially the adversarial-case count) still match reality; they're hand-written text, not generated, so they can drift the same way any other claim can, and this project's own standard should apply to its own README.
**Hash:** 28cb9189c870b5018fbf3252a14692bd8e56df96d5551e0b6d19bd5bab5ae81b

## 2026-07-20 · Made molt-init.py's TARGET_DIR optional, defaulting to the current directory, for a true GitHub copy-paste one-liner
**Decision:** `molt-init.py` required a target path argument, so `curl ... | python3 -` alone didn't work; someone would have had to know to append a path. Made TARGET_DIR optional: with no positional argument, it now defaults to the current directory. `--help` and `--force` still parse correctly with or without a path given. Updated the docstring's Usage section and README's install instructions to show the true one-liner: copy the command straight off the GitHub repo page, paste into a terminal or a Claude session, run, done, no editing required. Tested three forms directly: piped via stdin with zero arguments (sets up in cwd), piped with `--help` (still prints the docstring, doesn't try to scaffold), and run with an explicit path plus `--force` (still works exactly as before). Full audit and both benchmark scales (n=300, n=3000) still pass with no regressions.
**Reasoning:** Direct instruction: a visitor should be able to just copy a run command off GitHub and go, no argument to fill in first. A one-liner that still requires typing a path after it isn't actually a one-liner in practice.
**Reversible:** Yes. Behavior for anyone who does pass a path is unchanged; this only adds a fallback for the no-argument case.
**Review:** None.
**Hash:** 8541e12e3fe2bca4f73058b7cce1fe5f2911224d6e1dfaf0b5f8d9f71a21872b

## 2026-07-20 · Made molt-init.py fully self-contained, and added a check that catches it going stale
**Decision:** Rebuilt `molt-init.py` so it needs nothing else present: it now carries its own base64-encoded copies of molt-verify.py, molt-chain-append.py, molt-redact.py, the pre-commit hook, and the CI workflow, instead of copying them from sibling files in a checked-out repo. Copying just that one file anywhere, with nothing else, and running it now creates the entire setup in a single run, including memory/INDEX.md and memory/decisions.md, verified TRUSTWORTHY (with the expected placeholder WARN) against a target with only molt-init.py present and no other Molt file anywhere on disk. Base64 was chosen over embedding the files as raw Python string literals because those files have their own docstrings, regexes, and backslashes that would need escaping twice; base64 sidesteps that entirely. Dropped ARCHITECTURE.md and CLAUDE.local.md.example from the bootstrap (previously copied as siblings); they're documentation and an example, not load-bearing for a TRUSTWORTHY verdict, and the next-steps printout now points to the GitHub repo for them instead. Built `scripts/build-molt-init.py` as the one-time regenerator: decodes real files, re-encodes them, and patches molt-init.py's embed blocks by exact regex match, aborting with no changes if any expected block or source file is missing. Two copies of the same file that can silently drift apart is exactly the shape of bug this project has already found and fixed for AGENTS.md/CLAUDE.md and for mirrors, so added `check_init_embed_consistency` to molt-verify.py: decodes molt-init.py's own embedded blobs and byte-compares them against the real sibling files whenever both are present in the same root (skips cleanly, not a failure, if either side is missing, e.g. an adopter who only has molt-init.py itself). Verified the check both ways: a freshly rebuilt molt-init.py passes; a deliberately staled real molt-verify.py (edited without re-running the regenerator) fails with a clear message pointing at the fix. 2 new permanent adversarial cases (init_embed_correct, init_embed_stale); all 41 pass at n=300 and n=3000.
**Reasoning:** Direct instruction: someone with a clean setup and nothing else should be able to run one file and get the whole apex setup, not need a full repo checkout first. That's a real adoption-friction difference between "here's a GitHub link, go clone it" and "here's one file."
**Reversible:** Yes. molt-init.py's behavior for an adopter is unchanged (same files created, same templates, same idempotence); this only changes where its embedded framework copies come from. Reverting to sibling-copying would just mean restoring the old COPY_FILES logic.
**Review:** Whenever molt-verify.py, molt-chain-append.py, molt-redact.py, the pre-commit hook, or the CI workflow changes, run `python3 scripts/build-molt-init.py` before committing, then confirm with molt-verify.py that check_init_embed_consistency still passes.
**Hash:** 6d7b7370004f09a37b5b86b8ce461bb9bf16dbc38a3696e58db65acc93a8224d

## 2026-07-20 · Fixed a placeholder-detection false positive, found by this project's own log tripping its own check
**Decision:** The "leftover placeholder" check did a blind substring search for the literal text "YYYY-MM-DD" across the whole of memory/decisions.md and memory/INDEX.md. It false-positived the moment any entry's own prose merely mentioned that string (the molt-init.py entry, describing the placeholder convention its templates use), warning "example placeholder still present" when no real entry or index row actually used it as a date. Fixed to check whether a real parsed entry's date field, or a real index row's date cell, literally equals the placeholder, the same fix shape as the earlier nested-apex reserved-tag false positive (mentioning a tag in prose isn't the same as declaring it). Verified: the true-positive case (a fresh molt-init.py scaffold, which does have a real placeholder entry) still warns correctly; this project's own log no longer does. Added a permanent adversarial case. 40 cases pass at n=300 and n=3000.
**Reasoning:** Found by direct use, not a planned audit: asked how to fix the WARNs currently showing, and this one turned out to be the checker being wrong, not the log. Worth fixing immediately rather than telling someone to just ignore a false alarm from their own trust mechanism.
**Reversible:** Yes. Strictly narrows what counts as a placeholder; no previously-correct WARN or FAIL changes.
**Review:** None.
**Hash:** af2faaab94c9ad37bbcc5d864ceef7e3a5dac8ede0926649918e6f31e2161682

## 2026-07-20 · Built molt-init.py, and closed the concurrent-edit gap with union merge + a conflict-marker check
**Decision:** Built `molt-init.py`: copies the framework (molt-verify.py, molt-chain-append.py, molt-redact.py, the pre-commit hook, the CI workflow, ARCHITECTURE.md) into a target repo, generates a fresh starter CLAUDE.md/AGENTS.md/memory/INDEX.md/memory/decisions.md (a real example entry with the YYYY-MM-DD placeholder, not this project's own 18 entries), never overwrites an existing file without `--force`, and runs `molt-verify.py` against the target at the end. Tested against a fresh empty target (TRUSTWORTHY) and one with a pre-existing CLAUDE.md. That second test caught a real bug immediately: AGENTS.md was written from the script's own template instead of from whatever CLAUDE.md actually ended up containing, guaranteeing instant drift the moment someone adopts Molt into a project that already has a CLAUDE.md. Fixed to always derive AGENTS.md from the real, final CLAUDE.md content. Separately: added `.gitattributes` (`memory/decisions.md merge=union`) so two people or agent sessions appending different entries at the same anchor point merge automatically, no manual conflict resolution, tested directly with two real git branches. Added `check_merge_conflict_markers` to `molt-verify.py`, run first, before any other check, since a stray unresolved `<<<<<<<`/`=======`/`>>>>>>>` marker (from a genuine conflict a human resolved by hand and missed one) would otherwise corrupt every check downstream into a confusing cascade instead of one clear failure. Documented the one residual manual step honestly: union merge doesn't fix the hash chain (each side's Hash field commits to a different prev_hash), so a merge leaves entries missing hashes, exactly the existing "chain is incomplete" failure, fixed the existing way, re-run `molt-chain-append.py`, then commit. 3 new adversarial cases; all 39 pass at n=300 and n=3000.
**Reasoning:** Both directly requested. `molt-init.py` was named as the highest-leverage remaining thing for adoption friction; building it immediately surfaced a real bug the same way every other feature in this project has, only by actually testing the second, more realistic scenario (an existing project, not a fresh one) instead of just the happy path. The merge gap was flagged as lower urgency but still worth closing properly rather than leaving as a known limitation with no mitigation at all.
**Reversible:** Yes. `molt-init.py` is a new, standalone script nothing else depends on. `.gitattributes` and `check_merge_conflict_markers` are additive; the conflict-marker check only ever fires on content that should never be committed anyway.
**Review:** `molt-init.py`'s starter CLAUDE.md is a generic subset of this project's own rules (no `PROTOCOLS.md` reference, since that file isn't copied); revisit if adopters consistently want the full protocol file too. Union merge assumes every real edit is a pure top-of-file addition; if `memory/decisions.md` conventions ever allow edits elsewhere in the file, revisit whether `merge=union` is still the right default.
**Hash:** 472fac8bf6f8076326ca4daa9eafa7c57c5e48f380acc6c7a3a555af551c4829

## 2026-07-20 · Made verification mandatory (pre-commit hook + CI) and fixed a real nested-repo bug in the git anchor
**Decision:** Added `.githooks/pre-commit` (blocks a commit if `molt-verify.py` reports DRIFT DETECTED; one-time `git config core.hooksPath .githooks` per clone) and `.github/workflows/molt-verify.yml` (blocks a PR on the same condition, plus runs the adversarial benchmark at n=300 and n=3000 so a future edit that quietly weakens a check fails before merge). Tested the hook directly: a clean commit passes, a commit that strips the append-only anchor is blocked with the full audit output and an escape hatch (`--no-verify`) documented for genuine emergencies. While auditing for more gaps, found and fixed a real, confirmed bug in `check_git_anchor`: it tested for a literal `.git` directory inside ROOT, and used `HEAD:memory/decisions.md`, a repo-root-relative git path. Both silently no-op the moment Molt's root is a subdirectory of a larger repository (vendored into an existing project, a monorepo subdirectory) rather than the git top-level itself -- a real deployment shape, verified against an actual nested test repo, not a hypothetical. The check printed an innocuous "not a git repository -- skipped" in that case, silently disabling the CRITICAL mitigation exactly where nesting makes it likely someone would need it. Fixed by detecting git via `git rev-parse --is-inside-work-tree` and using the cwd-relative path form `HEAD:./memory/decisions.md`. Added a new adversarial base (`build_nested_git_synthetic_root`) and two permanent cases proving the nested case now behaves identically to the top-level case. All 38 cases pass at n=300 and n=3000.
**Reasoning:** Directly requested: make the audit something people can't skip, then keep auditing for more real gaps. Enforcement was the single highest-leverage thing separating "available" from "must-need," per that discussion. The nested-repo bug is exactly the kind of thing a fresh audit is supposed to catch: a check that looks correct and passes every existing test, but silently breaks in a deployment shape nobody had tested against yet.
**Reversible:** Yes. The hook and workflow are additive, opt-in for the hook (requires the one-time `core.hooksPath` config), CI enforcement is scoped to this repo only. The git-anchor fix strictly widens the cases it correctly detects; no previously-passing case's expected verdict changed.
**Review:** The pre-commit hook checks the working tree, not git's staged snapshot; an unstaged, unrelated change to a molt-tracked file could in theory make the hook's verdict not perfectly match what's about to be committed. Acceptable for now (matches common pre-commit hook practice elsewhere), revisit if it causes real false negatives in practice.
**Hash:** 8d75a694228ee22dc56dd1d40e5438083c85190d3b415f9e875154bdba4a04ad

## 2026-07-20 · Researched real agent-memory token-efficiency practice and added a Gist column, deliberately declining vector search
**Decision:** Researched how Mem0, Letta (MemGPT), and Zep hit their published token-efficiency numbers (Mem0: ~1.8K tokens/query vs ~26K full-context on LOCOMO): compress at write time, let a cheap index resolve most lookups via embeddings and a vector store. Adopted the principle, not the mechanism: added an optional `Gist` column to `memory/INDEX.md`, one line per entry, so most lookups resolve from the index without opening `decisions.md`. Backfilled all 15 existing entries. Declined embeddings/vector search: it's a dependency and a service (violates zero-dependency, no-network), and semantic similarity is approximate by construction, disqualifying for a system whose premise is exact proof, not fuzzy recall. Rewrote index parsing (`parse_index_table`) to be column-name driven instead of hardcoded `cells[2]`/`cells[3]`, so Gist didn't break Title/token parsing. Found and fixed two real regressions the rewrite caused: a too-strict version dropped a phantom row appended past a table's end (fixed with a positional fallback); and it exposed a genuine pre-existing gap in `check_handoffs`, which used to skip entirely when zero real handoff files existed, before ever checking whether INDEX.md's own handoffs table claimed a phantom one anyway (fixed to check regardless). Full detail on both fixes lives in the code comments at `parse_index_table` and `check_handoffs` in `molt-verify.py`. 4 new adversarial cases; all 36 pass at n=300 and n=3000. Non-adoption documented in `SCALE-SPEC.md`.
**Reasoning:** Requested directly: research what others do and incorporate what's genuinely useful. The useful part was the principle (compress once, verify honesty, let the index carry most lookups), not the technology; Molt already had progressive disclosure, just without a rich-enough index entry to resolve most lookups without a full fetch. The two masked gaps surfaced by tightening the parser are the same lesson this project keeps relearning: an accidental catch isn't a real one.
**Reversible:** Yes. Gist is opt-in, same pattern as the mirror check; `parse_index_table` is a strict superset of the old parser's coverage; the `check_handoffs` fix only adds detection where it previously silently skipped.
**Review:** Gist quality is a human judgment; `molt-verify.py` can only check "is it present," not "is it accurate." Revisit with a human spot-check if gists start drifting from what entries actually say. This entry itself was rewritten once, before hashing, to fit the 600-token budget the previous entry just introduced; that's the discipline working as intended, not an exception to it.
**Hash:** cbbbda4eaf353466c67e2f3aa9ef211da5971d407292bb3eb4fb497809cb09b2

## 2026-07-20 · Made token-efficiency claims self-verifying instead of just asserted
**Decision:** Measured this project's own `memory/INDEX.md` against the real size of each `decisions.md` entry and found every single `~tokens` estimate was 30-50% below the actual figure (e.g. the newest entry was claimed at ~520, actually ~820). Fixed every row to a real estimate. Then added `check_token_efficiency` to `molt-verify.py`: WARNs if an INDEX.md estimate drifts from a freshly computed one beyond tolerance (>25% and >50 tokens, to avoid noise on tiny entries), and separately WARNs if any single entry has grown past a 600-token verbosity budget, since progressive disclosure's whole value depends on both "the index tells the truth" and "one entry stays a cheap lookup." Added a short `<token_efficiency>` rule to `CLAUDE.md` naming this as a design constraint, not an afterthought. Found and fixed a real bug in the adversarial benchmark's own synthetic data while testing this: `build_synthetic_root` wrote a flat, hardcoded `~120` into every INDEX.md row regardless of actual entry size, which meant the benchmark's supposedly clean control case started failing this new check for a reason that had nothing to do with the real product, a benchmark-data problem, not a molt-verify.py problem. Fixed the generator to compute a real per-entry estimate with the same formula the check uses. Added two new permanent cases (`index_token_drift`, `oversized_entry`). All 34 cases pass at n=300 and n=3000.
**Reasoning:** Requested directly: make sure token consumption stays minimal and efficient. Progressive disclosure (read the index, not the whole log) is the entire mechanism this project relies on to stay cheap; if the numbers behind that decision are quietly wrong, the mechanism silently stops working while still looking fine. This is the same category of gap the mirror-drift and hash-chain-laundering findings were: a number or file that reads as trustworthy but was never actually checked against reality.
**Reversible:** Yes. `check_token_efficiency` is an additive, WARN-only check; the INDEX.md number corrections are data fixes with no downstream effect on any other check; the benchmark generator fix only changes synthetic test data, not real project files.
**Review:** The 600-token entry budget and the 25%/50-token drift tolerance are both first guesses, not measured optima; revisit if either produces false-positive noise in real use, or if entries keep needing to exceed the budget for genuinely complex fixes (in which case raise the budget deliberately, in a logged entry, rather than let it silently stop firing).
**Hash:** a9e778f0e55c31f1f9d3febe1a54f7bc9c0a3cd22d15287b0a5ec733c3001f5b

## 2026-07-20 · Fixed every finding from an external-style security review, including one CRITICAL
**Decision:** Ran a structured security review (severity: CRITICAL/HIGH/MEDIUM/LOW/INFO) against the whole project and fixed everything it found. CRITICAL: the hash chain has no anchor outside the log-writer's own control, so tampering with an old entry, stripping its Hash field and every one after it, then re-running `molt-chain-append.py`, produces a fresh, self-consistent chain that verifies TRUSTWORTHY -- verified this empirically twice before fixing anything. No local, dependency-free code change can fully close this (that needs a signing key or a remote with branch protection), so the honest fix is a real, tested, local mitigation plus documenting the boundary instead of implying it away: added `check_git_anchor` to `molt-verify.py`, which compares the working copy of `memory/decisions.md` against the last git commit and flags any change to historical content, catching the exact laundering attack as long as it hasn't been committed yet. Verified against the real attack in a scratch git repo: laundering while uncommitted now fails (`DRIFT DETECTED`); a legitimate new entry added on top, uncommitted, still passes clean; and, honestly, a laundering attack that gets fully committed is NOT caught by this check alone, stated plainly rather than oversold. HIGH: no sanctioned way existed to remove PII or a leaked secret from an append-only, hash-chained log. Added `molt-redact.py`: replaces one entry's four fields with a fixed placeholder, keeps its heading, appends a new top entry documenting the redaction itself, and rehashes the chain from that point forward. Found and fixed a real bug in my own first draft while testing it: the header-insertion regex only matched one `>` blockquote line, so the redaction entry landed inside the file's header instead of above the first real entry; fixed by inserting before the first actual `## ` heading instead of trying to parse the header's shape. MEDIUM: added a loud WARN when `.gitignore` contains a `!` negation pattern (previously silently unevaluated, now surfaced instead of silently trusted); added an explicit "what TRUSTWORTHY does and doesn't mean" and "what the hash chain does and doesn't prove" section to `ARCHITECTURE.md`. LOW: documented the importlib trust boundary in both `molt-chain-append.py` and `molt-redact.py`'s docstrings; added symlink guards to `check_mirror` and `find_nested_apex_files` so a symlinked file can't be silently read as if it were part of the intended tree. Added three permanent adversarial cases for the git-anchor mitigation (committed control, the laundering attack itself, a legitimate uncommitted addition) and one for the negation-pattern WARN. All 32 cases pass at n=300 and n=3000.
**Reasoning:** Requested directly: act as a product expert, find vulnerabilities, then fix all of them. The CRITICAL finding is a real, fundamental limitation of any self-contained local hash chain, not a bug to be quietly patched over; the honest response is a genuine local mitigation plus documenting exactly where it stops working, not a claim of non-repudiation this design can't back up. Every fix followed the same discipline as the rest of this project: reproduce the exact attack first, build the fix, prove empirically it now catches that attack, add it permanently to the adversarial benchmark, only then call it done.
**Reversible:** Yes. `check_git_anchor` and the gitignore-negation WARN are additive checks in `molt-verify.py`; `molt-redact.py` is a new, optional tool nothing else depends on; the symlink guards narrow existing behavior without changing any passing case's verdict.
**Review:** The CRITICAL finding's real fix (a signing key held outside the log-writer, or a remote with branch protection) is an infrastructure/process decision for whoever adopts Molt, not something this dependency-free script can set up unilaterally; revisit only if a team using this needs stronger guarantees than "local, best-effort, committed often."
**Hash:** 085434bb559ebaa97d095d4cf0ce31910a8dc17c4a881cdbd92fed53fd239898

## 2026-07-20 · Stress-tested the three newest features and fixed a real leak
**Decision:** Ran five new attacks specifically against hash-chaining, gitignore sanity, and nested apex support: deleting a whole already-hashed entry, swapping two already-hashed entries' physical positions, forging the oldest entry's genesis-linked hash, a `.gitignore` negation pattern, and a legitimate domain directory literally named `memory` (unrelated to Molt's own root `memory/`) containing a real `<never>` violation. The first three (deletion, reorder, forged genesis) were all correctly caught by the existing hash chain, no changes needed. The negation-pattern case is a known, already-disclosed limitation (see the CLAUDE.local.md decision's Review note), left as-is per the ponytail ladder. The `memory`-named directory case was a real, previously undisclosed leak: `check_nested_apex_consistency` pruned any directory literally named `memory` anywhere in the tree, not just the root's own `memory/`, so a real reserved-tag violation inside such a directory was silently invisible, reported as "no reserved-tag violations" when one plainly existed. Fixed `find_nested_apex_files` to skip the root's `memory/` by exact path, not by bare name, then re-verified the same attack now fails correctly and the real project's own `memory/` is still correctly skipped. Added the case permanently (`nested_memory_named_dir_shadow`). All 29 cases pass at n=300 and n=3000 (2.13s).
**Reasoning:** This was requested directly: stress-test everything built today across all three new features to make sure none of them leak. The `memory`-named-directory bug is the same shape as the "does this look right" trap the mirror-drift bug from earlier sessions taught: something that reads as reasonable code (skip a directory named after a special convention) can quietly break the exact guarantee it was supposed to provide, and only adversarial testing against a plausible real scenario caught it, not code review.
**Reversible:** Yes. One targeted fix to a directory-pruning condition; everything else in this pass required no changes.
**Review:** The gitignore negation-pattern gap remains open by choice, not oversight; building a real gitignore-spec-compliant matcher would be a large addition for a narrow, already-disclosed edge case. Revisit only if it's ever actually hit in practice.
**Hash:** 6f311730fe48cf91355b134923e774fbb6d75f10e362fc431c1bc99af7ba28f5

## 2026-07-20 · Added nested apex file support for monorepos, with a real working example
**Decision:** Added `check_nested_apex_consistency` to `molt-verify.py`: finds any `CLAUDE.md`/`AGENTS.md` inside a subdirectory (skipping `memory/`, `.git/`, and common non-domain dirs), fails if one redeclares `<never>`, `<verification>`, or `<before_declaring_done>` as an actual section rather than mentioning them in prose, and fails if a domain directory's own `CLAUDE.md`/`AGENTS.md` pair have drifted apart. Created `benchmarks/CLAUDE.md` and a byte-identical `benchmarks/AGENTS.md` as a real, working nested example, not just a documented idea. The first run against my own real example produced a false positive: the reserved-tag check matched the tag names mentioned in prose (`` `<never>` `` inside a sentence), not just real section redeclarations. Fixed by requiring the tag to be alone on its own line before counting it, then re-verified against a genuine violation (a real `<never>` section written into a nested file) to confirm the fix didn't just paper over the false positive. Added three permanent adversarial cases (correct nested pair, reserved-tag violation, drifted pair), plus updated `ARCHITECTURE.md`. All 28 cases pass at n=300 and n=3000 (2.06s).
**Reasoning:** This is the third and final piece of "works for people coding and building things": monorepos are the norm for real codebases, and a domain team needs to add its own rules without silently overriding the shared root governance. Catching the false positive against a real example, rather than only against synthetic test data, is the same lesson as the mirror-drift bug that shaped this whole project: test against something real before trusting a check.
**Reversible:** Yes. `check_nested_apex_consistency` no-ops if no nested apex files exist; deleting `benchmarks/CLAUDE.md`/`AGENTS.md` removes the example without affecting anything else.
**Review:** The reserved-tag detection is line-anchored, not markdown-aware; a tag written with unusual but valid indentation inside a code fence could theoretically still false-match. Revisit only if that's hit in practice.
**Hash:** 407d5d6711f432ffb754cda4ea6512aa2369f9fb8c3a12359c967217b0000dce

## 2026-07-20 · Added CLAUDE.local.md as a verified personal layer on top of the shared apex file
**Decision:** `.gitignore` now excludes `CLAUDE.local.md` and `AGENTS.local.md`; added `CLAUDE.local.md.example` as the copyable starting template. `CLAUDE.md`'s new `<personal_layer>` section documents the rule: personal files may only add preference (tone, verbosity), never weaken `<never>`, `<verification>`, or `<before_declaring_done>`, which stay identical for everyone. Added `check_gitignore_sanity` to `molt-verify.py`, checking two real mistakes: `CLAUDE.local.md` existing without being gitignored (personal preferences leak into the shared repo, fails even with no `.gitignore` at all, since that's the worst version of the same leak, not a reason to skip the check), and `CLAUDE.md`/`AGENTS.md`/`memory/` accidentally being gitignored (the shared rules silently stop syncing across the team). Added three permanent adversarial cases, all pass at n=300 and n=3000: a correctly-ignored personal file, a leaked one, and an accidentally-ignored shared file.
**Reasoning:** Verified this is a real, named 2026 convention before building it, not invented: shared rules committed to git, personal overrides in a gitignored `.local.md` file, is the documented pattern multiple current sources describe, with the shared-file-accidentally-gitignored mistake specifically called out as the most common real failure mode. This is what makes Molt usable by a team, not just an individual: two people can share one decision log and one set of shared rules while keeping their own personal tone preferences separate, without either leaking into the other.
**Reversible:** Yes. Deleting `CLAUDE.local.md.example` and the two `.gitignore` lines returns the project to solo-only shape; `check_gitignore_sanity` no-ops if no `.gitignore` and no local files exist.
**Review:** The gitignore pattern matcher is deliberately small (exact match plus simple fnmatch glob), not a full gitignore engine; a sufficiently exotic pattern (negation rules, `**` globs) could evade it. Revisit only if that gap is ever hit in practice, per the ponytail ladder.
**Hash:** 3904fda48928f93b5c7d4999475e416db632906b72b9018a092a6f0bb8a9bf91

## 2026-07-20 · Added hash-chained tamper-evidence for memory/decisions.md
**Decision:** Every entry can now carry an optional `**Hash:**` field: sha256(that entry's own required-field text + the previous, older entry's hash), oldest to newest, genesis hash all zeros. Added `check_hash_chain` to `molt-verify.py`: skips if no entries have a Hash field (opt-in, like the mirror check); fails if some entries have one and others don't (an incomplete chain gives false confidence); verifies the full chain and fails at the first break if any historical entry's hash doesn't match its recomputed value. Wrote `molt-chain-append.py`, a small standalone tool that backfills or extends the chain by pure deterministic hashing, never judging or summarizing content, consistent with the "human writes every decision" principle. Backfilled all 9 real entries in this project's own log. Verified the mechanism catches: tampering with the oldest entry (breaks the chain), and a chain missing one entry's hash (fails as incomplete). Added both as permanent adversarial cases, plus a hash_chain_control case, using a separate "chained" synthetic base so the other 19 cases stay unaffected. All 22 cases pass at n=300 and n=3000 (1.76s).
**Reasoning:** Verified this against a real, dated standard rather than inventing a scheme: the IETF's March 2026 "Agent Audit Trail" draft defines SHA-256 hash-chained records for autonomous AI systems, explicitly to satisfy EU AI Act Article 12's automatic event-logging requirement for high-risk systems, effective August 2026, this month. Before this, Molt's only tamper detection required maintaining an external mirror; an unmirrored log could have an old entry edited in place with nothing catching it. This closes that gap without requiring a mirror at all, the single biggest remaining trust hole this project had.
**Reversible:** Yes. The Hash fields are additive lines within existing entries; deleting `molt-chain-append.py` and the Hash fields returns the log to its prior, still-valid shape. `check_hash_chain` no-ops cleanly if no entries have a hash.
**Review:** The canonical hash text currently excludes only the Hash line itself, not any other formatting; if an entry's non-substantive whitespace is ever reformatted by a future tool (e.g. a markdown linter), that would also break the chain even though nothing meaningful changed. Revisit if that turns out to happen in practice; it hasn't yet, and being strict by default is the safer failure mode for a tamper-evidence feature.
**Hash:** d48e5f4de88101a3acbbddd7c50f467f638c0042ce8c935f97fb5cb1a5779df9

## 2026-07-20 · Added AGENTS.md as a verified cross-tool mirror of CLAUDE.md
**Decision:** Created `AGENTS.md`, a byte-identical copy of `CLAUDE.md`, so tools that don't auto-load a file named `CLAUDE.md`, Cursor, GitHub Copilot, Codex CLI, Gemini CLI, Aider, Windsurf, Zed, all of which read `AGENTS.md` natively as the open 2026 cross-tool convention, see the exact same rules Claude Code and Cowork do. Added `check_agents_md` to `molt-verify.py`: skips (informational, not a failure) if `AGENTS.md` doesn't exist, since it's optional; fails if it exists and has drifted from `CLAUDE.md`. Updated `CLAUDE.md`'s own `<how_to_load_this>` and `ARCHITECTURE.md` to explain the split. Added two permanent adversarial cases, a correct byte-identical `AGENTS.md` (should pass) and a drifted one (should fail); both do, at n=300 and n=3000.
**Reasoning:** Verified the actual current convention by search rather than assuming from training knowledge, since tool conventions like this change and a wrong guess here would ship a file nobody's tooling actually reads. The original plan (separate pointer files per tool) was wrong; the real fix is one file, `AGENTS.md`, using the exact mirror mechanism Molt already had, applied to a single file instead of a directory. This is what makes "works no matter which model" actually true across tools, not just across Claude models, which is what the project already claimed but didn't fully deliver.
**Reversible:** Yes. `AGENTS.md` is a plain file; deleting it just means those tools stop getting Molt's rules, nothing else depends on it.
**Review:** If Cursor's `.cursor/rules/*.mdc` glob-scoped format or Copilot's glob patterns are ever needed specifically, add those as additional tool-specific files at that point, not before, per the ponytail ladder. `AGENTS.md` alone covers the common case today.
**Hash:** 8600bfb9a8031892de67916d5b4dc2d14064847d24456c66117b1bec83bb0c52

## 2026-07-20 · Fixed a crash and a robustness gap found by testing common, non-adversarial breakage
**Decision:** Tested `molt-verify.py` against ordinary real-world breakage rather than deliberate attacks: Windows CRLF line endings (already handled fine), a decision title containing a literal `|` (already correctly flagged as drift), a completely empty `decisions.md` (already correctly fails hard, no fix needed), and a binary file accidentally dropped into a mirror directory. The last one crashed the whole script with an unhandled `UnicodeDecodeError` and a raw Python traceback instead of a verdict. Fixed `check_mirror` to catch decode/IO errors per file and report them as a normal failure. Also added a general safety net in `main()`: every check now runs inside a try/except, so any future unexpected exception in any check becomes a reported failure instead of a crash, and the remaining checks still run. Added a permanent adversarial case (`binary_file_in_mirror`) for this.
**Reasoning:** A tool whose entire purpose is "always deliver a verdict, never assume" failing by crashing is a worse outcome than failing by reporting DRIFT DETECTED. The other three common cases (CRLF, pipe-in-title, empty log) were tested and found to already behave correctly, worth recording as confirmed-safe rather than silently assumed safe.
**Reversible:** Yes. The mirror fix is a try/except around one comparison; the main() safety net wraps existing check calls without changing their logic.
**Review:** If a check ever legitimately needs to abort the whole run (not just fail loudly), the safety net would currently swallow that into a generic failure message instead. Revisit if that distinction ever matters in practice; it hasn't yet.
**Hash:** 7cc8d3b55954457f35e7e5b9731ed0c2c6861bab7d4ed6d8b790d44133990030

## 2026-07-20 · Found and fixed two new real gaps via a second adversarial pass, plus a crash in the first pass's own fix
**Decision:** Ran five new, previously untried attacks against `molt-verify.py`. Two were caught already (a lookalike Unicode date separator, and a zero-width space hidden in an index title), now added as permanent regression cases. Two were real, new breaks: a forged entry dated with full-width Unicode digits (visually "2026-07-20" but not real ASCII digits) always compared as "newest" regardless of position, since Python's `\d` matches Unicode digit lookalikes; and an entry could be smuggled in below the append-only anchor comment, since nothing checked that the anchor was actually the last thing in the file. Fixed both: `ISO_DATE` now requires literal `[0-9]`, not `\d`; the anchor check now fails if anything real follows the anchor's own comment close (`-->`). While building the permanent test for the first of these two, the earlier duplicate-entry fix crashed with `TypeError: '<' not supported between instances of 'NoneType' and 'str'`, because sorting `(date, title)` keys breaks when an entry's date is `None` (a legitimately tolerated case for malformed headings). Fixed the sort key to handle `None` explicitly. All 17 adversarial cases now pass at n=300 and n=3000 (0.66s).
**Reasoning:** The first adversarial round tested obviously-broken data. This round tested data that looks fine to a human but exploits a specific assumption in the code (visual similarity, string-comparison semantics, "presence" versus "position" of the anchor). Finding two real breaks on a second, different-angle pass confirms the earlier finding wasn't a fluke: new categories of attack keep finding new real gaps, which is exactly why this needs to stay a repeatable benchmark, not a one-time pass.
**Reversible:** Yes. Three targeted fixes (date regex, anchor position check, sort key), each independently revertible.
**Review:** The homoglyph fix relies on the specific character `·` (U+00B7); a different-looking separator character used consistently instead of `·` would still parse as a valid heading with `date=None`, which is already the correctly-handled fallback, not a silent bypass. Revisit only if a heading format ever needs to support separators other than `·`.
**Hash:** ec6912f2abcbc645662de4cf423e96d0b46ea26397a2e9ca93c4479b8438c1fa

## 2026-07-20 · Made same-day entry ordering visible instead of silently unenforceable
**Decision:** `molt-verify.py`'s newest-first check now warns when two or more log entries share the same date, naming them, since date alone can't verify their relative order and the check previously passed silently either way. Chose a warning, not a failure: there's no additional signal (no timestamp) to prove one order over another, so failing would be asserting certainty the data doesn't support. Added a permanent adversarial case (`same_day_entries`) confirming this surfaces correctly. Running the updated script against the real project's own log immediately surfaced a true instance: today's session logged 4 entries all dated 2026-07-20, and their relative order is in fact only preserved by file position, not provable by date.
**Reasoning:** This was a named gap from the last review: two same-day entries passed as "newest-first" with no real check behind that claim. Silently trusting file order without saying so is exactly the kind of unearned confidence the whole system exists to avoid. Turning it into a visible warning doesn't fabricate an answer, it tells the truth: this specific thing can't be verified with the data currently kept.
**Reversible:** Yes. One check added, nothing about the file format changed.
**Review:** If entries ever need a provable intra-day order, add a time component or a per-day sequence suffix to the date field. Until then, this warning is the honest ceiling of what date-only entries can verify.
**Hash:** e3f78ce137e9ff02f81d9ae1abb3928c68616ca46dcf24a199c1190be6123298

## 2026-07-20 · Fixed parser fragility and added handoffs/domain-buckets structural checks
**Decision:** Two changes to `molt-verify.py`. First, `parse_decisions` now only treats a `## `-looking line as a new entry if it's preceded by a blank line (or is the first line), so a heading pasted or written inside an entry's own body, a quoted code review, a copied doc excerpt, can no longer silently split or merge real entries. Second, added `check_index_sections` (warns if `INDEX.md`'s `handoffs/` or `domain buckets` sections vanish) and `check_handoffs` (fails if a real file in `memory/handoffs/` has no matching row in `INDEX.md`'s new handoffs table, or a row points at a file that doesn't exist). Gave `INDEX.md`'s handoffs section an actual `| Date | File | ~tokens |` table so there's something concrete to check against. Extended `benchmarks/adversarial/run.py` with three new cases: a heading pasted mid-body (expected to stay TRUSTWORTHY, proving the parser fix), a phantom handoff row, and a missing handoff row (both expected to fail). All 10 cases pass at n=300 and n=3000 (0.35s).
**Reasoning:** Both gaps were named directly after the last round: the parser was never tested against content that merely resembles its own structure, and `INDEX.md` had been making two structural promises, handoffs and domain buckets, that nothing enforced. Fixing the parser without a permanent regression case for it would repeat the exact mistake that made the fail-vs-warn gap possible in the first place, so the new benchmark case exists specifically to make sure this fix outlives this session.
**Reversible:** Yes. Both changes are additive checks plus one parsing guard; nothing about the existing file formats changed except `INDEX.md` gaining an empty table under handoffs.
**Review:** Domain buckets currently only gets a presence check (the heading still exists), not a structural cross-check, because it has no real directory to check against yet, only free-form prose. Revisit once domain buckets get an actual file-per-bucket convention; until then a real cross-check there would be inventing rigor the format doesn't support.
**Hash:** 44bb53e60020b2976119909ec5e399471ac2d0a2097c3d0862ddab871f68edf5

## 2026-07-20 · Made the adversarial benchmark permanent at benchmarks/adversarial/run.py
**Decision:** Committed the one-off manual adversarial test as a real, re-runnable script: `benchmarks/adversarial/run.py`. It builds a synthetic decision log at any size, runs six corruption injectors (phantom index row, missing index row, out-of-order entry, malformed entry, missing anchor, mirror drift) plus a clean control, and asserts `molt-verify.py`'s exit code and verdict match what's expected for each. Confirmed it reproduces today's manual results, then ran it again at n=3000 (versus the original manual pass at n=300): all 7 cases still behave correctly, in 0.24s wall time. No dependencies beyond stdlib; nothing it touches lives outside a temp directory.
**Reasoning:** A benchmark that only exists as shell history is not a benchmark, it's a story about one afternoon. The whole point raised earlier was that this needed to be reusable so a future regression, for example someone reverting the fail-vs-warn fix, gets caught automatically instead of rediscovered by hand. Running it at 3000 also closes a named gap from the same review: the original manual pass never actually tested the "thousands" scale the spec named, this run did, and found no degradation.
**Reversible:** Yes. It's one script under `benchmarks/`; deleting it costs nothing at runtime and doesn't affect `molt-verify.py` or `memory/`.
**Review:** Wire this into a pre-commit hook or CI alongside `molt-verify.py` itself once one exists for this repo. Re-run after any future change to `molt-verify.py`'s parsing or check logic, not just after changes to `memory/`.
**Hash:** c2ecf76e0087a37880cd656e7f3960dc5cda2cb3588bf1c4e836f6c1559280d7

## 2026-07-20 · Ran the adversarial benchmark from SCALE-SPEC.md and tightened two soft checks to hard failures
**Decision:** Built a synthetic 300-entry decisions.md/INDEX.md and injected six corruption types (phantom index row, missing index row, out-of-order entry, malformed entry missing a required field, missing append-only anchor, mirror drift). Four were already caught as failures. Two, a malformed entry missing a required field and a missing append-only anchor, only produced warnings, exit 0, "TRUSTWORTHY (with notes)." Changed both from `rep.warn` to `rep.fail` in `molt-verify.py` so a memory missing a required field or its ordering guard no longer passes as trustworthy. Re-ran both cases against the fixed script: both now correctly fail (exit 1, DRIFT DETECTED). Re-ran the real project's own log afterward; still passes clean.
**Reasoning:** `molt-verify.py`'s own doc comment says "Exit code 0 = trustworthy." An entry silently missing its Reversible field, or a log that's lost the anchor protecting append-only ordering, is the same category of lie as a phantom index row, it just wasn't being scored that way. This is exactly what item 5 of `SCALE-SPEC.md` (adversarial verification benchmark) was for: proving the trust claim under deliberate corruption before writing any new capacity features, not after. Found a real severity gap on the very first run.
**Reversible:** Yes. Two `rep.warn(...)` calls became `rep.fail(...)`; reverting is a two-line change. No data format changed, no existing entries affected.
**Review:** If this proves too strict in practice, for example a mirror-less setup where the anchor genuinely doesn't matter yet, revisit severity per-check rather than reverting both. Placeholder-date and no-index-rows checks were deliberately left as warnings; they're hygiene, not provenance, and weren't part of this pass.
**Hash:** 376c6d3a8f7808931dc24f3a632f4d1701a4bf57d082c20cc77f40d3c5294cad

## 2026-07-20 · Scoped a scale spec to compete with agentmemory, Headroom, and claude-mem without abandoning trust-first design
**Decision:** Wrote `SCALE-SPEC.md`, a design-only spec (no code yet) adding four capabilities, sharded decisions log, a deterministic (non-embedding) recall index, an optional checksummed digest layer, and human-confirmed assisted drafting via `memory/pending/`, plus a fifth item: an adversarial verification benchmark that injects known corruption types into a large synthetic log and asserts `molt-verify.py` catches every one. Scope was explicitly kept to "trust-first, add just enough scale," not a feature-for-feature race with agentmemory's embeddings or Headroom's compression ratios.
**Reasoning:** Researching three real competitors (agentmemory: 11.6k stars, semantic recall at scale; Headroom: tool-output compression; claude-mem: AI-driven auto-summarization) showed Molt's current design has never been tested past one real decision entry, and the original entry's own named trigger ("if entries ever grow into the thousands... revisit") was close to firing for real. None of the three competitors have an equivalent to `molt-verify.py`; that stays the moat. The adversarial benchmark item exists because a comparison table against competitors proves nothing, a harness that shows the audit catching deliberate corruption at scale is the actual receipt.
**Reversible:** Yes. Nothing in `CLAUDE.md`, `memory/`, or `molt-verify.py` was changed to implement this; it's a spec only. Discarding `SCALE-SPEC.md` costs nothing at runtime.
**Review:** Before writing any code from this spec, build the adversarial benchmark (item 5) first and confirm it passes against the *current*, unsharded design. Only add sharding, the recall index, or the digest layer if the benchmark or real entry count actually forces it, per the ponytail ladder.
**Hash:** d751a9fd89efab5f5b3b7259e07e7b537d238b2449c4b02ddbae2a571ad5327c

## 2026-07-19 · Rebuilt Molt around a self-audit that catches the agent's own drift
**Decision:** Made the self-audit (`molt-verify.py`) the center of the system rather than an add-on. The audit checks that the index matches the log exactly, that the log is newest-first and well-formed, and, if a mirror exists, that every mirrored file byte-matches its source. Kept the whole thing dependency-free (Python stdlib only) so nothing it relies on can itself rot.
**Reasoning:** An agent memory you can't trust is worse than none, because you act on it. Most agent-memory projects compete on capacity (embeddings, graphs, retrieval); none prove they aren't lying to you. That gap is the whole reason to pick this. The mirror check specifically exists because a hand-written "mirror" note once gained a sentence the real log never contained, and only a byte-diff caught it. Mechanical verification beats good intentions.
**Reversible:** Yes. The audit is one file; delete it and you still have a plain, working memory system. Nothing else depends on it at runtime.
**Review:** If entries ever grow into the thousands, or the memory spans many machines, revisit whether a real database belongs underneath. That is the named trigger, not a date.
**Hash:** 2421e6abf4f3a37a246bd6f051a8d6f228cf908b516eac004d9df2cd4e36611e

<!-- Add new entries above this line. Keep the oldest at the bottom. -->
